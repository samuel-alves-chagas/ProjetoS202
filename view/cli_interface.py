# from pprintpp import pprint as pp
# from model.entitysDAO import EntitysDAO
from utils.utils import divider, optionStr


def view(connection):

    # dao = EntitysDAO(connection)

    while 1:
        option = input(optionStr)
        divider()
        if option == '1':
            divider()

        elif option == '2':
            divider()

        elif option == '3':
            divider()

        else:
            break
